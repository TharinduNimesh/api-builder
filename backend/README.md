# Backend (TypeScript + Express)

This folder contains a minimal TypeScript + Express scaffold following the LogRocket tutorial.

Quick start:

1. cd backend
2. npm install
3. cp .env.example .env
4. npm run dev

Build for production:

1. npm run build
2. npm start

Notes: No controllers or routes are added yet per request.
