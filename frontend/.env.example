# Example environment variables for the frontend (Vite)
# Copy this file to `.env` or `.env.local` and update values as needed.
# Vite exposes variables prefixed with `VITE_` via `import.meta.env`.

# Base URL for the backend API used by the frontend.
# Example: `http://localhost:3000` or `https://api.example.com` for production.
VITE_API_BASE=http://localhost:3000
